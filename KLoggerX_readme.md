# KLoggerX 飞书云文档1:1复刻项目开发说明书

版本：v1.0  

更新时间：2026-03-16  

核心适配：Cursor编辑器全量开发、Linux私有化部署、前后端分离架构、1:1复刻飞书云文档全功能

---

## 一、项目概述

### 1.1 项目定位

KLoggerX 是一款**1:1复刻飞书云文档**的私有化在线文档与知识管理平台，完全对标飞书云文档的交互、功能、视觉体验，实现文档全生命周期管理、多人实时协作、结构化知识沉淀，满足企业/团队私有化部署需求。

### 1.2 复刻核心目标

1. 100%覆盖飞书云文档「我的文档库」全量功能，无遗漏还原操作流程与交互逻辑

2. 复刻飞书全类型在线编辑能力（富文本文档、表格、幻灯片、多维表格、思维笔记、问卷）

3. 实现飞书同级别的多人实时协作、权限管控、版本管理、知识库、全文检索核心能力

4. 提供开箱即用的Linux私有化部署方案，支持一键启动

### 1.3 硬性技术约束

- 架构：前后端完全分离

- 前端：Vue3 + Vite + TypeScript 技术栈

- 后端：双选型，优先Go语言（Gin框架），备选Python（FastAPI框架）

- 部署：原生Linux部署 + Docker容器化部署双支持

- 所有代码可直接在Cursor编辑器中完成全量开发、调试、构建

---

## 二、技术栈选型（严格符合需求）

### 2.1 前端技术栈（Vue3生态，1:1还原飞书视觉与交互）

|技术/依赖|选型说明|对标飞书对应能力|
|---|---|---|
|核心框架|Vue 3.4+ + Vite 5+ + TypeScript 5+|飞书云文档前端核心架构，保证高性能与类型安全|
|UI组件库|Element Plus|还原飞书企业级UI交互风格，适配办公场景|
|路由/状态|Vue Router 4 + Pinia|页面路由管理、全局状态管理（用户、文档、协作状态）|
|富文本编辑器|Tiptap 2 + ProseMirror|1:1复刻飞书富文本文档编辑能力，支持块级编辑、富文本全格式|
|表格编辑器|Luckysheet|复刻飞书表格/多维表格能力，支持公式、数据透视、多人协作|
|幻灯片编辑器|PPTist|复刻飞书幻灯片编辑、演示能力|
|思维导图|@vue-flow/core|复刻飞书思维笔记/思维导图能力|
|实时协作|Yjs + WebSocket|实现飞书同级别的多人实时编辑、光标同步、内容冲突解决|
|文档预览|PDF.js + LibreOffice|实现PDF、Office文档在线预览，格式转换|
|网络请求|Axios|接口请求封装，拦截器、错误处理|
### 2.2 后端技术栈（双选型，优先Go版）

#### 优先选型：Go语言版（高性能、Linux部署友好、适配协作高并发）

|技术/依赖|选型说明|
|---|---|
|核心框架|Go 1.22+ + Gin 框架|
|ORM|GORM 2.0|
|数据库|MySQL 8.0|
|缓存/协作状态|Redis 7.0|
|鉴权|JWT + Casbin|
|WebSocket|Gorilla WebSocket|
|对象存储|MinIO|
|文档处理|LibreOffice|
#### 备选选型：Python版（快速开发、易调试）

|技术/依赖|选型说明|
|---|---|
|核心框架|Python 3.10+ + FastAPI|
|ORM|SQLAlchemy 2.0|
|数据库/缓存|MySQL 8.0 + Redis 7.0|
|鉴权|PyJWT + Casbin|
|WebSocket|FastAPI 原生WebSocket|
|对象存储/文档处理|MinIO + LibreOffice|
### 2.3 部署环境要求

- 最低服务器配置：2核4G Linux服务器（CentOS 7+/Ubuntu 20.04+）

- 完整部署依赖：Docker、Docker Compose（一键部署）、Nginx、LibreOffice

- 支持x86/arm架构Linux系统，完全私有化部署，无外网依赖

---

## 三、1:1复刻完整功能清单（全覆盖飞书云文档全量核心功能）

### 3.1 核心模块1：我的文档库（100%还原官方功能）

|功能点|详细需求（完全对标飞书逻辑）|
|---|---|
|文档库入口|1. 侧边栏常驻「我的文档库」，支持与云盘、知识库快速切换；2. 顶部导航栏固定云文档入口，点击直达文档库；3. 支持目录树折叠/展开，与飞书完全一致的交互逻辑|
|新建页面|完全还原2种新建方式：<br>1. 方式1：在我的文档库目录树，选中目标位置，点击右侧「+」图标，选择新建类型（文档、表格、思维笔记、幻灯片、多维表格、问卷）<br>2. 方式2：点击页面右上角「+」图标，先选文档类型，再选择新建目标位置<br>3. 内置飞书同款模板库（会议记录、周报、项目方案等），新建时可直接选用|
|迁入已有文档|1. 支持批量迁入已有文档，单次最大20篇；2. 迁入流程：打开文档编辑权限→选择目标位置→选择迁入文档→确认迁入→实时展示迁入进度；3. 跨所有者迁入：需向文档原所有者、原位置所有者发起申请，对方同意后完成迁入|
|上传和导入|1. 支持本地文件上传，单文件最大10GB；2. 支持「导入为在线文档」：本地Word/Excel等文件直接转换为平台在线文档，格式无损；3. 支持批量上传、文件夹拖拽上传，保留原目录结构|
|快捷方式管理|1. 支持将其他文档的快捷方式添加到我的文档库；2. 支持将快捷方式设置为子页面，完整继承原文档的内容更新与权限；3. 快捷方式图标、交互与飞书完全对齐|
|置顶与收藏|1. 置顶：选中文档/文件夹，点击「置顶」，置顶内容在侧边栏「置顶文档」区展示，支持一键取消置顶；2. 收藏：支持将文档加入收藏，收藏内容在云文档主页「收藏」列表展示，支持快速筛选与取消收藏|
|分享页面|1. 支持分享页面的「可管理/可编辑/可查看/只读」4级权限设置；2. 支持链接分享、邀请协作者，分享范围可设置：仅协作者可见、组织内可见、互联网公开可见；3. 子页面权限继承：分享父页面时，可选择是否同步分享子页面，对方仅能看到被分享的页面及子页面；4. 严格遵循飞书限制：不支持分享整个我的文档库|
|转移所有权|1. 支持将我的文档库内的文档所有权转移给组织内其他用户；2. 转移流程：选中文档→选择转移对象→选择是否保留自己的协作权限→确认转移；3. 飞书同款限制：不支持转移给外部用户；转移后文档自动从原所有者文档库移除，新所有者获得完整管理权限|
|页面管理|1. 创建副本：支持创建文档副本，可选择是否同步创建子页面副本，副本不继承原文档协作者权限；2. 移动页面：支持将文档/文件夹移动到文档库其他位置、知识库、共享空间，支持目录树拖拽移动；3. 重命名、复制链接、导出等右键菜单全量功能|
|回收站与恢复|1. 支持将文档移入回收站，30天内可恢复，30天后自动彻底删除；2. 支持主动彻底删除文档；3. 回收站支持还原、批量彻底删除、筛选功能，完全对齐飞书逻辑|
### 3.2 核心模块2：全类型在线文档编辑能力

1. 富文本文档：完全复刻飞书文档的块级编辑能力，支持标题、正文、列表、表格、图片、附件、代码块、引用、任务列表、高亮、@人、评论、插入思维导图/表格/幻灯片

2. 实时保存：编辑内容每30秒自动保存，无感知同步，支持手动保存，避免内容丢失

3. 版本历史：自动保存每一次修改记录，记录修改人、修改时间，支持查看历史版本、对比版本差异、一键回滚到任意历史版本

4. 离线编辑：支持断网时离线编辑，联网后自动同步修改内容

5. 导入导出：支持导入Word/Excel/PPT/PDF/Markdown文件，导出为Word/PDF/Markdown/图片格式

6. 评论与批注：支持选中内容添加评论、批注，支持@协作者，支持评论回复、解决、删除，完全对齐飞书评论交互

### 3.3 核心模块3：多人实时协作能力

1. 多人同时编辑：支持无限人数同时编辑同一篇文档，实时同步所有编辑内容

2. 协作者状态显示：实时显示在线协作者列表、头像、光标位置、编辑选区，不同协作者光标显示不同颜色与昵称

3. 协作者权限实时生效：权限变更后实时生效，无权限用户立即无法编辑/查看

4. 冲突自动解决：基于Yjs实现CRDT算法，自动解决多人编辑内容冲突，无锁编辑，与飞书协作体验完全一致

5. 操作通知：@协作者、评论、权限变更时，实时推送消息通知给对应用户

### 3.4 核心模块4：权限与组织架构管理

1. 组织架构管理：支持部门/用户管理，支持批量导入用户，支持角色设置（管理员/普通成员/只读成员）

2. 细粒度RBAC权限：4级核心权限（所有者/可管理/可编辑/可查看），支持文档级、文件夹级、页面级权限设置

3. 权限继承：子页面/子文件夹默认继承父级权限，支持单独覆盖设置权限

4. 外部协作：支持添加组织外用户为协作者，设置单独权限，支持设置外部协作白名单

5. 操作日志：完整记录文档的所有操作（查看、编辑、权限变更、分享），支持审计追溯

### 3.5 核心模块5：知识库与知识管理

1. 知识库创建：支持创建团队知识库，设置知识库管理员、成员、访问权限

2. 结构化知识管理：支持知识库多级目录树，文档结构化归档，支持目录拖拽排序

3. 知识库权限：支持知识库级、目录级、文档级三级权限管控，与飞书知识库权限逻辑完全对齐

4. 知识发布：支持文档草稿、发布状态，支持发布审核流程，支持历史版本回溯

5. 全文检索：支持知识库内、全平台全文检索，可检索文档标题、正文内容、附件文本，支持关键词高亮、按相关性/时间排序

---

## 四、项目目录结构（Cursor可直接生成）

### 4.1 前端项目目录（Vue3 + Vite）

```Plain Text

kloggerx-web/
├── public/                 # 静态资源
│   ├── favicon.ico
│   └── template/           # 文档模板文件
├── src/
│   ├── api/                # 接口请求封装
│   │   ├── modules/        # 分模块接口
│   │   │   ├── user.ts     # 用户/组织架构接口
│   │   │   ├── document.ts # 文档库/文档操作接口
│   │   │   ├── auth.ts     # 权限相关接口
│   │   │   ├── collaborate.ts # 实时协作接口
│   │   │   ├── knowledge.ts  # 知识库相关接口
│   │   │   └── file.ts     # 文件上传/导入接口
│   │   ├── request.ts      # Axios请求封装（拦截器、错误处理）
│   │   └── websocket.ts    # WebSocket长连接封装
│   ├── assets/             # 静态资源
│   │   ├── images/
│   │   ├── styles/         # 全局样式、飞书同款主题样式
│   │   └── fonts/
│   ├── components/         # 全局公共组件
│   │   ├── common/         # 通用组件（按钮、弹窗、右键菜单、面包屑）
│   │   ├── editor/         # 编辑器核心组件
│   │   │   ├── rich-text/  # 富文本编辑器（Tiptap封装，对标飞书文档）
│   │   │   ├── sheet/      # 表格编辑器（Luckysheet封装）
│   │   │   ├── slide/      # 幻灯片编辑器（PPTist封装）
│   │   │   └── mind/       # 思维笔记编辑器
│   │   ├── document-tree/  # 文档目录树组件（1:1还原飞书交互）
│   │   ├── permission/     # 权限设置组件
│   │   └── share/          # 分享/协作者管理组件
│   ├── router/             # 路由配置
│   │   ├── index.ts        # 路由入口
│   │   └── modules/        # 分模块路由
│   ├── store/              # Pinia全局状态管理
│   │   ├── modules/
│   │   │   ├── user.ts     # 用户/组织状态
│   │   │   ├── document.ts # 文档状态
│   │   │   ├── auth.ts     # 权限状态
│   │   │   └── collaborate.ts # 协作状态
│   │   └── index.ts
│   ├── hooks/              # 自定义Vue Hooks
│   │   ├── useDocument.ts  # 文档操作Hook
│   │   ├── useAuth.ts      # 权限校验Hook
│   │   ├── useCollaborate.ts # 实时协作Hook
│   │   └── useWebSocket.ts # WebSocket Hook
│   ├── utils/              # 工具函数
│   ├── views/              # 页面组件
│   │   ├── layout/         # 全局布局（1:1还原飞书侧边栏+顶部导航）
│   │   ├── document/       # 文档库相关页面（我的文档库、编辑页、回收站、收藏）
│   │   ├── knowledge/      # 知识库相关页面
│   │   ├── user/           # 登录、注册、个人设置页面
│   │   └── common/         # 404、500等通用页面
│   ├── types/              # TypeScript全量类型定义
│   ├── App.vue             # 根组件
│   └── main.ts             # 项目入口
├── .env.development        # 开发环境配置
├── .env.production         # 生产环境配置
├── vite.config.ts          # Vite配置
├── tsconfig.json           # TypeScript配置
├── package.json            # 依赖管理
└── README.md
```

### 4.2 后端项目目录（Go Gin 优先版）

```Plain Text

kloggerx-server/
├── cmd/                    # 项目入口
│   └── server/
│       └── main.go         # 主程序入口
├── api/                    # 接口层
│   ├── v1/                 # v1版本接口
│   │   ├── user.go         # 用户/组织架构接口
│   │   ├── document.go     # 文档库/文档接口
│   │   ├── auth.go         # 权限接口
│   │   ├── collaborate.go  # 实时协作接口
│   │   ├── knowledge.go    # 知识库接口
│   │   └── file.go         # 文件上传/导入接口
│   └── router.go           # 路由总入口
├── config/                 # 配置文件
│   ├── config.yaml         # 主配置文件
│   └── config.go           # 配置结构体
├── internal/               # 核心业务代码
│   ├── service/            # 业务逻辑层
│   │   ├── user_service.go
│   │   ├── document_service.go
│   │   ├── auth_service.go
│   │   ├── collaborate_service.go
│   │   ├── knowledge_service.go
│   │   └── file_service.go
│   ├── repository/         # 数据访问层
│   │   ├── mysql/          # MySQL数据操作
│   │   └── redis/          # Redis缓存操作
│   ├── model/              # 数据模型（MySQL表结构对应）
│   │   ├── user.go
│   │   ├── document.go
│   │   ├── permission.go
│   │   ├── collaborate.go
│   │   ├── knowledge.go
│   │   └── file.go
│   ├── middleware/         # 中间件
│   │   ├── jwt_auth.go     # JWT鉴权中间件
│   │   ├── casbin_auth.go  # 权限校验中间件
│   │   ├── logger.go       # 日志中间件
│   │   └── cors.go         # 跨域中间件
│   ├── ws/                 # WebSocket实时协作服务
│   │   ├── server.go       # WebSocket服务入口
│   │   ├── handler.go      # 消息处理逻辑
│   │   └── client.go       # 客户端管理
│   └── pkg/                # 内部工具包
│       ├── jwt/            # JWT工具
│       ├── minio/          # MinIO对象存储工具
│       ├── logger/         # 日志工具
│       ├── utils/          # 通用工具
│       └── libreoffice/    # 文档转换工具
├── deploy/                 # 部署相关
│   ├── docker/             # Docker配置
│   │   ├── Dockerfile
│   │   └── docker-compose.yml
│   ├── nginx/              # Nginx配置
│   └── systemd/            # Linux服务配置
├── go.mod                  # Go依赖管理
├── go.sum
└── README.md
```

---

## 五、Linux私有化部署指南

### 5.1 服务器环境要求

- 系统：CentOS 7+/Ubuntu 20.04+，x86/arm架构

- 最低配置：2核4G，推荐4核8G

- 依赖：Docker 24+、Docker Compose v2+（一键部署），或Nginx、MySQL、Redis、MinIO、LibreOffice（原生部署）

### 5.2 Docker Compose一键部署（推荐，开箱即用）

1. 服务器创建部署目录

```Bash

mkdir -p /data/kloggerx && cd /data/kloggerx
```

1. 上传`docker-compose.yml`文件，内容包含前端、后端、MySQL、Redis、MinIO、LibreOffice全服务

2. 一键启动所有服务

```Bash

docker-compose up -d
```

1. 访问地址：`http://服务器IP:80`，默认管理员账号`admin@kloggerx.com`，密码`123456`

### 5.3 原生Linux部署步骤

1. 安装基础依赖：MySQL、Redis、MinIO、Nginx、LibreOffice

2. 前端打包：`npm run build`，将打包后的`dist`目录上传到服务器`/data/kloggerx/web`

3. 后端编译：`GOOS=linux GOARCH=amd64 go build -o kloggerx-server cmd/server/main.go`，上传二进制文件到服务器

4. 配置Nginx反向代理，代理前端静态资源与后端API接口

5. 配置systemd服务，实现后端服务开机自启、进程守护

6. 初始化数据库，导入基础表结构与默认数据

7. 启动服务，访问测试

---

## 六、Cursor专属开发提示词集

### 6.1 全量项目生成提示词（一次性生成完整项目框架）

直接粘贴到Cursor的Chat面板，即可生成完整的前后端项目框架与核心代码

```Plain Text

你现在需要帮我1:1复刻飞书云文档，开发一个名为KLoggerX的在线文档与知识管理平台，严格遵循以下所有要求，生成完整的可运行的前后端分离项目代码，所有代码必须符合规范，可直接在Linux系统部署运行。

=== 硬性技术要求 ===
1.  前端：必须使用Vue3 + Vite + TypeScript，UI组件库使用Element Plus，必须严格按照我提供的前端目录结构生成代码，1:1还原飞书云文档的视觉与交互
2.  后端：优先使用Go 1.22+ + Gin框架，必须严格按照我提供的Go后端目录结构生成代码，数据库使用MySQL 8.0，缓存使用Redis 7.0，权限使用JWT + Casbin，对象存储使用MinIO，实时协作使用WebSocket + Yjs
3.  必须实现前后端完全分离，所有接口必须有完整的请求/响应类型定义，错误处理，鉴权逻辑
4.  必须提供完整的Linux部署方案，包含Docker Compose一键部署配置、Nginx配置、systemd服务配置
5.  所有代码必须有清晰的注释，符合行业开发规范，可直接编译、运行、部署，无语法错误

=== 核心功能要求（必须100%覆盖，无遗漏） ===
1.  完全还原飞书云文档「我的文档库」全量功能，包括：2种新建页面方式、迁入已有文档、上传和导入、快捷方式管理、置顶和收藏、分享页面、转移所有权、创建副本、移动页面、删除与回收站恢复，所有交互逻辑必须与飞书云文档完全一致
2.  实现全类型在线编辑能力：富文本文档、表格、幻灯片、多维表格、思维笔记，支持实时保存、版本历史、回滚、评论与批注、导入导出
3.  实现飞书同级别的多人实时协作能力：多人同时编辑、光标同步、内容实时同步、协作者状态显示、冲突自动解决
4.  实现完整的RBAC细粒度权限管控，4级权限（所有者/可管理/可编辑/可查看），支持组织架构、部门/用户管理、权限继承、外部协作
5.  实现飞书同款知识库功能，包括知识库创建、结构化目录管理、三级权限管控、全文检索
6.  所有功能必须完全对标飞书云文档，交互、流程、视觉风格必须1:1还原，不得简化功能

=== 输出要求 ===
1.  先输出完整的项目架构说明，再分别输出前端项目全量代码、后端项目全量代码
2.  每个文件的代码必须完整，可直接复制到Cursor中运行，不得省略核心逻辑
3.  必须包含完整的依赖配置文件（package.json、go.mod）、配置文件、部署配置文件
4.  必须包含完整的MySQL表结构初始化SQL语句
5.  必须包含详细的开发、部署、运行说明
```

### 6.2 分阶段精准开发提示词（分步开发，Cursor逐段执行）

#### 阶段1：项目初始化提示词

```Plain Text

帮我初始化KLoggerX项目的前后端基础框架，严格遵循以下要求：
1.  前端：Vue3 + Vite + TypeScript + Element Plus，生成完整的项目目录结构，配置路由、Pinia状态管理、Axios请求封装、全局样式、基础布局（1:1还原飞书云文档的侧边栏+顶部导航布局）
2.  后端：Go + Gin框架，生成完整的项目目录结构，配置MySQL、Redis连接、JWT鉴权中间件、CORS跨域中间件、路由框架、日志系统
3.  输出所有文件的完整代码，可直接在Cursor中运行，无语法错误
4.  包含完整的依赖配置文件，可直接安装依赖启动项目
```

#### 阶段2：我的文档库核心功能开发提示词

```Plain Text

帮我开发KLoggerX的「我的文档库」核心功能，严格1:1还原飞书云文档的功能与交互，要求如下：
1.  完全实现附件截图内的所有功能：文档目录树、2种新建页面方式、迁入已有文档、上传和导入、快捷方式、置顶收藏、分享页面、转移所有权、创建副本、移动页面、删除与回收站
2.  前端实现完整的页面交互，包括目录树拖拽、右键菜单、弹窗、分页、搜索，交互逻辑与飞书完全一致
3.  后端实现对应的所有接口，包括文档CRUD、权限校验、文件上传、目录管理、回收站管理
4.  所有接口必须有权限校验，用户只能操作自己有权限的文档
5.  输出完整的前后端代码，包含所有组件、接口、数据模型，可直接运行
```

#### 阶段3：在线编辑器与实时协作开发提示词

```Plain Text

帮我开发KLoggerX的在线编辑器与多人实时协作功能，对标飞书云文档，要求如下：
1.  前端集成Tiptap富文本编辑器，1:1还原飞书文档的块级编辑能力，支持所有富文本格式、@人、评论、图片/附件插入
2.  集成Yjs + WebSocket实现多人实时协作，支持多人同时编辑、光标同步、内容实时同步、冲突自动解决、协作者状态显示
3.  实现版本历史功能，自动保存修改记录，支持版本对比、一键回滚
4.  后端实现WebSocket服务，管理协作客户端、消息转发、状态同步、版本保存
5.  输出完整的前后端代码，可直接运行实现多人实时协作编辑
```

#### 阶段4：权限系统与知识库开发提示词

```Plain Text

帮我开发KLoggerX的权限系统与知识库功能，严格对标飞书云文档，要求如下：
1.  基于Casbin实现RBAC细粒度权限管控，4级权限（所有者/可管理/可编辑/可查看），支持文档级、文件夹级、页面级权限设置，支持权限继承
2.  实现组织架构与用户管理，支持部门创建、用户导入、角色设置
3.  实现飞书同款知识库功能，包括知识库创建、成员管理、多级目录管理、文档发布、全文检索
4.  所有接口必须有完整的权限校验，无权限用户无法访问对应资源
5.  输出完整的前后端代码、权限模型、数据库表结构，可直接运行
```

#### 阶段5：部署配置开发提示词

```Plain Text

帮我生成KLoggerX项目的完整Linux部署配置，要求如下：
1.  生成前端Dockerfile、后端Dockerfile，支持多阶段构建，镜像体积最小化
2.  生成完整的docker-compose.yml文件，包含前端、后端、MySQL、Redis、MinIO、LibreOffice所有服务，支持一键启动
3.  生成Nginx配置文件，实现前端静态资源代理、后端API反向代理、WebSocket长连接支持
4.  生成Linux systemd服务配置文件，实现后端服务开机自启、进程守护
5.  生成详细的部署步骤文档，包含环境准备、安装命令、启动方式、常见问题解决
```

### 6.3 代码调试与优化提示词

```Plain Text

帮我调试KLoggerX项目的代码，解决以下问题，同时优化代码性能与规范：
1.  检查所有代码的语法错误、逻辑漏洞，修复所有Bug
2.  优化前端页面性能，提升编辑器加载速度、协作同步速度
3.  优化后端接口性能，添加缓存、限流，提升高并发场景下的稳定性
4.  完善所有接口的错误处理、异常捕获，添加完整的日志
5.  优化代码规范，统一命名风格，补充必要的注释，提升代码可维护性
6.  输出修改后的完整代码，标注修改点与优化说明
```

---

## 七、补充说明

1. 本说明书所有内容均适配Cursor编辑器的AI开发能力，所有提示词可直接粘贴使用，无需额外调整

2. 技术选型均采用开源、可私有化部署的组件，无商用限制，完全满足企业内网部署需求

3. 功能清单完全对齐飞书云文档最新官方功能，可根据实际需求裁剪或扩展
> （注：文档部分内容可能由 AI 生成）